
export default {
    state: {
        userInfo: '',
        token: ''
    },
    mutations: {
    },
    actions: {
        // 登录
        handleLogin ({ commit }, { userNo, password }) {
            return new Promise((resolve, reject) => {

            })
        },
        // 退出登录
        handleLoginOut ({ commit }) {
            return new Promise((resolve, reject) => {

            })
        }
    }
}
